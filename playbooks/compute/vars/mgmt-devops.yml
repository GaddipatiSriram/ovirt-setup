---
# MGMT DevOps VMs - 3 VMs on MGMT_DEVOPS network (10.10.2.0/24)
# Usage: ansible-playbook deploy_vms.yml -e @vars/mgmt-devops.yml

# VM naming
vm_prefix: "mgmt-devops"
vm_count: 3

# VM size
vm_size: "medium"

# Network - VLAN 11
network_name: "Mgmt-DevOps-Net"

# Template
template_name: "fedora-41-mgmt"

# Cloud-init
root_password: "unix"
admin_user: "admin"
admin_password: "unix"
domain: "engatwork.com"
